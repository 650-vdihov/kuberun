apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .Release.Name }}-clubs
  labels:
    app: clubs
    release: {{ .Release.Name }}
spec:
  selector:
    matchLabels:
      app: clubs
      release: {{ .Release.Name }}
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
