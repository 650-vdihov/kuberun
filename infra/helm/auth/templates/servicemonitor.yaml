apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .Release.Name }}-auth
  labels:
    app: auth
    release: {{ .Release.Name }}
spec:
  selector:
    matchLabels:
      app: auth
      release: {{ .Release.Name }}
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
