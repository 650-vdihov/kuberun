apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .Release.Name }}-activity
  labels:
    app: activity
    release: {{ .Release.Name }}
spec:
  selector:
    matchLabels:
      app: activity
      release: {{ .Release.Name }}
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
